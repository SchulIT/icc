{% set details = details|default(false) %}
{% if details %}
    {{ studyGroups|studygroups(true) }}
{% else %}
    {% if studyGroups|length > 10 %}{{ 'label.study_groups_count'|trans({'%count%': studyGroups|length}) }}
    {% else %}{{ studyGroups|studygroups(true) }}{% endif %}
{% endif %}
