<?php

namespace App\Book;

use App\Entity\Grade;
use App\Entity\Teacher;
use App\Entity\Tuition;
use DateTime;

class MissingEntryResolver {

    private function resolveMissingEntriesForTuitions(array $tuitions, DateTime $start, DateTime $end): array {

    }

    public function resolveMissingEntriesForTeacher(Teacher $teacher, DateTime $start, DateTime $end): array {

    }

    public function resolveMissingEntriesForTuition(Tuition $tuition, DateTime $start, DateTime $end): array {

    }

    public function resolveMissingEntriesForGrade(Grade $grade, DateTime $start, DateTime $end): array {

    }
}